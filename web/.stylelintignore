# Lint only hand-authored CSS. Everything below is either build output, a
# faithful WordPress mirror, or minified vendor CSS — not ours to restyle.
node_modules/
dist/
.astro/
.wrangler/
_mirror/
public/assets/theme/
public/assets/plugins/
public/assets/includes/
**/*.min.css

# Chrome components reproduce the WordPress theme markup verbatim (incl. vendor
# inline styles) — not hand-authored CSS, so not ours to restyle. ConsentBanner
# (real authored <style>) stays linted.
src/components/SiteHeader.astro
src/components/SiteFooter.astro
src/components/OffCanvasNav.astro
src/components/SiteNav.astro
src/components/SiteHead.astro
