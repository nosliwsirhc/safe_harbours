---
interface Props {
  title: string;
  intro?: string;
}
const { title, intro } = Astro.props;
---
<header class="page-head">
  <div class="wrap">
    <h1>{title}</h1>
    {intro && <p class="lead">{intro}</p>}
  </div>
</header>
