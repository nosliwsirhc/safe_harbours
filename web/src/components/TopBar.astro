---
import { site } from '../data/site';
---
<div class="topbar">
  <div class="wrap">
    <span class="urgent"><span class="dot"></span>Foster homes urgently needed — children across Ontario are waiting</span>
    <span class="links">
      <a href={site.emailHref}>{site.email}</a>
      <a href={site.phoneHref}>{site.phone}</a>
    </span>
  </div>
</div>
